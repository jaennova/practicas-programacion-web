function suma(num1, num2) {
    return num1 + num1
}
function saludo(nombre) {
    document.write("saludos " + nombre);
}
function calcularArea(base, altura) {

}

function texto(texto, color, tamanio) {
    document.write("<font color =" + color + "size = " + tamanio + "/>" + texto);
}